context('Header', () => {
    it('should show a header component with title', () => {
        cy.visit('http://localhost:3000')
    })
})

context('Footer', () => {
    it('should show a footer component', () => {
        cy.visit('http://localhost:3000')
    })
})